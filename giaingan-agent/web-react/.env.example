# Endpoint API backend cho dashboard goi vao (Vite dung tien to VITE_)
# Local:
VITE_API_BASE=http://localhost:8000
# Khi deploy: thay bang public endpoint agent tren GreenNode AgentBase
# VITE_API_BASE=https://<agent-endpoint>.greennode.ai
